name: farmer_labour_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  
  # Existing dependencies - KEEP THESE
  provider: ^6.1.1
  supabase_flutter: ^2.1.1
  url_launcher: ^6.3.2
  http: ^1.1.0          # ✅ Already have - for backend API calls
  intl: ^0.19.0         # ✅ Already have - for date formatting
  flutter_sms: ^2.3.3   # ✅ Already have - for device SMS
     # ✅ Already have - for state management
  postgres: ^2.6.1      # ✅ Already have
  bcrypt: 1.1.3         # ✅ Already have
  shared_preferences: ^2.2.2  # ✅ Already have
  cupertino_icons: ^1.0.8     # ✅ Already have

  # NEW DEPENDENCIES TO ADD
  fluttertoast: ^8.2.2        # For toast notifications
  # flutter_dotenv: ^5.1.0    # Optional - for environment variables

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true